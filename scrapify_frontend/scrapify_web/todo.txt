


checkJWT in storage
checkRefreshJWT in storage

handleLogin
    => fetch API
        => Success -> store JWT in storage -> handle User context
        include JWT in axios

Logged in
    => listen to error response
    => check refresh Token isvalid => refresh token => store 

Guard
    => check JWT in storage
    else redirect to login

Logout
    => clear access, refresh JWT in storage